namespace Monomino.Constants;

public static class Dimensions
{
    public const int TileWidth = 28;
    public const int TileHeight = 28;

    public const int DefaultWindowWidth = 1280;
    public const int DefaultWindowHeight = 720;

    public const double AspectRatio = (double)DefaultWindowHeight / (double)DefaultWindowWidth;
}
